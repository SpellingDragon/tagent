## 0. 取证实证（前置，全部落定后才进实现）

- [x] 0.1 grep 实证 F1 历史快照渲染点：定位 service/gate 下实际把入站消息加工为历史的文件与函数（含提及剥离、图片/GIF 空行渲染的具体代码位置），读回贴证
- [x] 0.2 grep 实证 persona 文件落点：定位真实 persona 提示词文件路径、结构与其现有段落锚点（供 F3 增补），读回贴证
- [x] 0.3 取证机器人自回复入史：读日志/代码核实机器人自身群聊回复当前是否进入历史快照；输出结论（进/不进 + 证据行号），与 20:51 @NANA 答豆包榜单、20:53 @Misaki 答非所问两例"回复很奇怪"疑云合并归因
- [x] 0.4 取证历史 char cap：核实 20:51 时 G411 的提问是否被历史 char cap 裁掉（读日志中实际送模型的快照内容），输出结论 + 证据；据此给出 F4 立项/不立项判定
- [x] 0.5 实证 profile 注册表与窗口帧关联的数据形态：确认 username↔openid 双源映射（既有 profile 注册表结构 + 同窗口帧关联的存在性与查询方式），读回贴证

## 1. F1 入站历史保真（依赖 0.1、0.5）

- [x] 1.1 实证待改文件行尾字节形态（CRLF/LF），确定行级手术参数；实证结果记入变更说明
- [x] 1.2 实现提及渲染：入站 `<@openid>` → `@显示名`，显示名解析按窗口帧关联 → profile 注册表 → 回退 `@成员` 三级优先；查不到绝不残留 `<@...>` 裸标记
- [x] 1.3 实现媒体占位：图片 → `[图片]`、GIF → `[GIF]`，不再出现空行渲染
- [x] 1.4 若 0.3 结论为"自回复不进史"：在渲染层补机器人自回复入史；若已在史：读回贴证并跳过本步（在报账中注明）
- [x] 1.5 为 1.2/1.3/1.4 写单测（含提及可解析、提及不可解析回退、图片、GIF、自回复入史五类用例），go test 全绿

## 2. F2 出站真提及（依赖 0.5，与 1.2 共用映射源）

- [x] 2.1 在 robot.go 发送路径实现发送前 `@显示名` → `<@openid>` 替换：反查走窗口帧关联 → profile 注册表双源，未命中保留原文；正则按已知显示名全集逐字转义匹配（防显示名含特殊字符）
- [x] 2.2 为 2.1 写单测（命中替换、未命中保留原文、其余文本逐字不变、显示名含正则特殊字符四类用例），go test 全绿

## 3. F3 persona 软规则（依赖 0.2）

- [x] 3.1 按 0.2 实证锚点向 persona 增补指代消解规则：指代类问题只点名上下文明确出现过且与行为直接关联的人，材料不足 MUST 反问澄清，不得以活跃度/存在感点名；增补前读回全文确认不与既有段落冲突
- [x] 3.2 增补后读回 persona 全文验证：规则落点正确、既有段落语义未变、CRLF 行尾未被破坏（行级手术）
  贴证：persona 增补后读回断言全过（指代规则落点 react_agent.go persona 段尾部、既有段落字节未动、行尾保持原文）；09-12 22:3x 增补链产出。

## 4. F4 char cap 缓解（条件任务：仅当 0.4 结论为"确被裁掉"时立项）

- [x] 4.1 （0.4 否定分支）取证结论：G411 提问未被 char cap 裁掉——20:51 快照 48 条时间戳行完整含 [20:39] G411 提问行（证据：robot.log.20260912_210001.gz 内 20:51:08 SetChatHistory 行）。按计划跳过缓解实施，结论已落档 design.md Open Questions。

## 5. 测试、上线与验收（依赖  包 1-3 全部完成，4 视结论）

- [x] 5.1 全量 go test 全绿（禁止跳过失败用例）；新增用例与本轮全部改动文件清单一并报账
- [x] 5.2 go build 出新 exe；执行 restart.sh 上线；验收三件套：单实例 + 运行中 exe md5 = 盘上新件 md5 + 心跳正常，读回输出贴证
- [x] 5.3 群聊实测回验（重放 2026-09-12 场景或等效场景）：指代类问题点名正确（不再 @NANA 式乱猜）、出站 @ 呈现真提及（蓝字）、图片/GIF 历史占位可见；结果读回贴证

## 6. 提交与推送

- [x] 6.1 git 提交并推送 main：仅含本轮变更文件（不含 config.yaml），提交前 git status/diff 读回核对暂存内容
- [x] 6.2 推送后读回远端 main 最新 commit（fetch + log/diff 比对），确认与本地一致，不凭 push success 回执断言落地
