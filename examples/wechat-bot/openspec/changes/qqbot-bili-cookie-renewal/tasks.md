## 1. 前置状态核验（读回证据）

- [ ] 1.1 读回 config.yaml，确认 `safe_mode: true` 已写入且格式有效
- [ ] 1.2 读回机器人重启后日志，统计重连失败计数，确认 = 0（无风暴基线）
- [ ] 1.3 核对 ~/qqlogin-tool/main.go 存在且含 gen/verify 两子命令入口（读文件确认，非听声称）

## 2. 离线构建扫码工具

- [ ] 2.1 在 ~/qqlogin-tool/ 补齐 go.sum（使用已有模块缓存，不触网拉新依赖）
- [ ] 2.2 以 `GOROOT=/usr/local/go.bak GOPROXY=off` 显式隔离环境构建，产物二进制存在且 `--help`/子命令列表可执行回显
- [ ] 2.3 构建产物路径与构建命令记录留痕（供复现与回溯）

## 3. 扫码登录与凭据写入

- [ ] 3.1 运行 `gen` 生成登录二维码 PNG，确认文件存在且大小合理（非空、可打开）
- [ ] 3.2 二维码经微信投递给用户，记录投递时间；等待扫码确认
- [ ] 3.3 外层套 `timeout` 运行 `verify`，写入 cookie.json
- [ ] 3.4 读回 cookie.json：确认写入成功、字段完整（含凭据字段）、时间戳为本次操作时间（即非 32 天前旧值）

## 4. 汇报与恢复

- [ ] 4.1 汇报续期结果（cookie 更新证据 + 日志摘录）给用户，明确请求确认关闭 safe_mode
- [ ] 4.2 获用户明确点头后：改写 config.yaml 为 `safe_mode: false`，读回确认
- [ ] 4.3 正常模式重启机器人，观察一段窗口期日志，确认B站连接正常、重连风暴未复发（读日志证据）

## 5. 收尾

- [ ] 5.1 更新本计划：各步骤产物证据（路径/日志行）随报账附上
- [ ] 5.2 归档计划（全部 [x] 且证据齐备后）
