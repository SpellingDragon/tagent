## 0. 评估准备

- [ ] 0.1 锚定变更范围：`git log --oneline 23016d8..8bab6a4` 输出 55 commits 清单，`git diff --stat 23016d8..8bab6a4` 输出 134 files 变更分布，将五簇变更各自对应的 commit 区间/文件落定（产物：commit→簇映射表）
- [ ] 0.2 提取各簇设计意图：通读五簇变更的核心 commit message 与关键 diff（strictyaml/RL 认证/fsync/StopLoop/SIGUSR2、WAL fallback/RebuildExecutor/孤儿裁决/SystemAlert、prompt 解耦、min_gap/knowledge 检索/settle、spill-finder），逐簇写出"设计意图一句话"作为判定基准
- [ ] 0.3 评估基线快照：记录当前 HEAD、运行中服务状态、最近一次冥想/settle 时间戳，作为后续证据比对基准（命令：`git rev-parse HEAD`、服务状态检查、logpath 尾部时间戳）

## 1. 立即演练层（可主动触发的能力面验证）

### 簇① 远端 v0.2.0 合流

- [x] 1.1 strictyaml 配置面验证：演练配置加载路径，确认 strictyaml 解析生效且热载通道不回退旧解析（验收标准：配置加载成功且无 fallback 告警；证据：演练命令输出贴入报告）
- [x] 1.2 RL 认证验证：按 v0.2.0 认证流程演练一次完整认证（验收标准：认证通过/拒绝路径均符合设计；证据：认证调用输出）
- [x] 1.3 fsync 持久化验证：演练一次写路径确认 fsync 生效（验收标准：写后强杀进程数据不丢；证据：演练前后数据对比）
- [ ] 1.4 StopLoop 语义验证：演练触发 StopLoop 场景（验收标准：循环按新语义停止、无残留 goroutine/进程；证据：演练日志片段）
- [ ] 1.5 SIGUSR2 信号处理验证：向运行中进程发送 SIGUSR2，观察预期行为（验收标准：信号触发预期动作（如配置热载/dump）；证据：`kill -USR2 <pid>` 前后日志对比）

### 簇② fix-reincarnation 10 特性（无生产证据部分）

- [ ] 1.6 EmitSystemAlert 8 处接线演练：定位 8 处接线点，逐一构造触发条件或至少在测试环境触发 1 处，确认 SystemAlert 真实发出（验收标准：≥1 处真实触发 + 8 处接线点路径正确性核对；证据：触发的 alert 消息记录 + 代码路径核对表）

### 簇⑤ 运维工具

- [x] 1.7 spill-finder glob 缺陷回归：运行 spill-finder（含 glob 场景），确认本轮修复后 glob 匹配正确（验收标准：已知缺陷场景 glob 命中预期文件、无误报/漏报；证据：运行输出对比缺陷复现记录）

### 簇③ prompt 解耦+死链修复

- [ ] 1.8 热载通道 gen2+ 稳定性确认：演练一次 prompt 热载（gen2+ 通道），确认解耦后新 prompt 生效且无死链（验收标准：热载后新 prompt 内容在下次调用可见、死链清单全修复；证据：热载前后 prompt 内容 diff + 死链检查输出）

## 2. 自然观察点层（等自然事件验证）

### 簇④ 冥想变更

- [ ] 2.1 min_gap 1h 生效观察：确认配置 min_gap=1h 已生效（验收标准：连续两次冥想间隔 ≥1h，无违反；证据：logpath 中冥想 spawn 时间戳序列）
- [ ] 2.2 knowledge 检索 base-dir 漂移缺陷排查：复现两次调用异常（一次空输出、一次 ENOENT），定位 base-dir 漂移根因（验收标准：给出根因定位 + 漂移条件复现；证据：异常调用日志 + 根因分析；此为缺陷登记不修复）
- 2.3 settle 泄漏写侧验收（**时间门控：`8bab6a4` 部署后需等下一次冥想 spawn**）：
  - [ ] 2.3.1 确认 `8bab6a4` 部署时间已满且下一次冥想 spawn 已发生（验收标准：logpath 出现部署后首个冥想 spawn 事件；证据：spawn 时间戳 > 部署时间戳）
  - [ ] 2.3.2 settle 写侧泄漏验收：检查该次冥想 spawn 后 settle 路径无泄漏（验收标准：settle 写侧无泄漏征兆（文件句柄/内存/目录残留）；证据：spawn 后的 settle 相关日志与状态检查输出）

### 簇①⑤ 生产观察补强

- [ ] 2.4 远端 v0.2.0 合流生产观察：若 1.1–1.5 演练全部通过但生产无真实流量验证，登记为 🟡 待验并标注观察点（验收标准：观察点定义明确（何种生产事件可佐证）；证据：观察点登记表）

## 3. 固化审计层（已有证据归档固化）

### 簇② fix-reincarnation 10 特性（已有生产证据部分）

- [ ] 3.1 证据定位：WAL fallback / RebuildExecutor / 孤儿裁决 / SystemAlert 中已有生产证据的特性，逐一定位证据在日志/事件流中的位置（验收标准：每特性 ≥1 条生产证据定位（文件+行号/时间戳）；证据：证据定位表）
- [ ] 3.2 证据固化归档：将 3.1 定位的证据摘要+原始片段固化进评估报告附录（验收标准：附录含每特性证据片段；证据：报告附录章节）

### 簇③ prompt 解耦

- [ ] 3.3 prompt 活体验证证据固化：簇③已活体验证，将其验证时间/方式/结果固化进报告（验收标准：报告含簇③活体证据章节；证据：报告章节）

## 4. 汇总判定与报告落盘

- [x] 4.1 五簇三级判定汇总：按"✅ 有效（intent+test+prod 证据齐）/ 🟡 待验（test 绿但缺生产证据）/ 🔴 缺陷（实测不过）"对五簇逐一判定并给出依据（验收标准：五簇判定齐且每判定附证据链引用；证据：报告判定章节）
- [x] 4.2 缺陷清单登记：将发现的缺陷（至少含 knowledge base-dir 漂移）登记为独立条目（验收标准：缺陷含现象/复现/根因/建议处理计划名；证据：报告缺陷章节）
- [ ] 4.3 待验项跟踪表：将全部 🟡 判定项整理为跟踪表（含观察点、预期佐证事件、超期升级条件）（验收标准：跟踪表含上述字段；证据：报告跟踪表章节）
- [x] 4.4 评估报告落盘：将完整评估报告写入 `openspec/changes/audit-24h-change-validity/report.md`（验收标准：文件存在且含五簇判定/缺陷清单/待验跟踪表/证据附录四部分；证据：`read_file openspec/changes/audit-24h-change-validity/report.md` 非空）
