# Changes Ledger — 活跃变更集重编账本

> 2026-09-05 整体重编:活跃区从 6 个变更(含 163 个停滞 ≥7 周的僵尸任务)收敛为
> "1 张地图(tagent-evolution-roadmap) + 1 个活跃变更(hybrid-semantic-recall)"。
> 归档区(archive/)为历史账本,内容不改写;本文件集中记录本次重编的裁决与证据。

## 本次裁决记录

| 变更 | 重编前状态 | 裁决 | 证据 |
|---|---|---|---|
| agent-package-rewrite | 78/78 全完成未归档 | ARCHIVE(纯手续) | tasks 全勾选;纯重构无行为变更 |
| unified-event-consumer-and-async-tool | 36/62,停滞 7 周 | ARCHIVE-AS-IS(实质完成) | responseCh 双通道已修(examples/wechat-bot/main.go:245-254 注释 "fixes the responseCh deadlock bug",仅存注释);ActionTool 中间态噪音已消失(全库无 waiting_async_response/handleStateChange);剩余 26 任务主题被归档变更 async-result-delivery / unified-event-projection / async-task-management 以更演进设计覆盖 |
| memory-storage-production-hardening | 0/102,停滞 8 周 | ARCHIVE-AS-IS(母计划已被拆散执行) | 其整合的 4 个母 change 均已独立归档(archive/2026-06-20-*、2026-06-21-*);Phase1 KVRange 已实现(memory/rustviking_client.go:189);Phase3 FileBackend 已移除;Phase4 LLM 摘要以 compress SummaryModel 形态落地;Phase2 硬化组件(Tombstone/Lifecycle/Compactor)已接线运行(tagent.go resolveMemoryStore);残余文档类任务并入 roadmap P0 |
| risk-mitigation-semantic-recall | 0/61,未动工 | REVIVE → 重编为 hybrid-semantic-recall | 核心前提仍成立(SearchByEmbedding 为 stub:memory/in_memory_store.go:306、memory/segment_store.go:762);但阶段 3(12 任务)挂载的 ProjectionOrganizer 已被 retire-projection-organizer(2026-07-24 归档)退役;向量方案详述(异步 EmbeddingWorker/选择性生成/zhipu embedding-3)质量高,整体吸收进新变更 |
| tagent-deep-source-evaluation | 空目录(0 工件) | REJECT(删壳) | 对应产出为两篇评审文档(现位于 docs/.dev/,不随 git 走),处置见 roadmap 任务 6.2 |
| tagent-evolution-roadmap | 新建未批准 | 保留,修订为 backlog 地图 + 治理框架 | P0 改直做清单;P1 指向 hybrid-semantic-recall;不再为已覆盖主题另起变更 |

## 当前活跃集

| 变更 | 角色 | 状态 |
|---|---|---|
| tagent-evolution-roadmap | 参考地图 + roadmap-governance 治理规格(门禁/CONFIRM/自驱闭环)+ D6 交接须知 | 待批准(不急执行) |
| hybrid-semantic-recall | P1 语义检索执行体(risk-mitigation-semantic-recall 的重编版;2026-09-05 追加组 8 向量链路可观测) | 工件就绪,待用户放行开工 |
| observability-tracing | P1.5 trace 骨架执行体(turn root span / task span link / 轨迹互链;spike-first;不依赖 P1 可先行) | 工件就绪(2026-09-05 立项),待放行 |

## 追加裁决记录

| 日期 | 裁决 | 依据 |
|---|---|---|
| 2026-09-05 | trace/telemetry 引入拆两层:向量链路 span/metric 归 hybrid-semantic-recall 组 8;turn 级骨架+轨迹互链立新变更 observability-tracing(roadmap P1.5) | 用户裁决方案 B;探索证据:框架层 span 已自动埋点(llmflow/functioncall)、wechat-bot 已有 OTLP 开关、缺口全在 tagent 自有层(turn 无 root span、EventKey 未进属性、spawn/settle 无 link、轨迹与 trace 零关联) |
| 2026-09-05 | 评审断言"示例写了未发布型号 glm-5.3"标记为**疑似误判,不列入修复项** | 本机实测:zhipu coding 端点(open.bigmodel.cn/api/coding/paas/v4)+ ZAI_API_KEY 持续真实可用(real-LLM 测试/MCP probe 均走通);glm-5.3 应为该端点可用型号,评审判断疑为其知识截止所致。接手者勿"修复"此项;P0 执行时如型号确不可用再议(验证方式:一次真实 API 调用) |
| 2026-09-05 | 登记并存规划:docs/.dev/tagent下一步迭代设计报告.md(305KB,五方向 D1-D5 + Wave0-3,可编码粒度)与本 roadmap(P0-P4)+两执行体**并存作交接参考**,不预设谁为准,接手后讨论迭代调和 | 用户裁决。已知待调和项见 roadmap design D6「并存规划」:①D2 向量存储方案分歧(报告 VectorStore 装饰器 vs hybrid 变更 内存索引+RustViking KV 序列化);②报告 D3 常驻可靠性是 roadmap 缺口;③D1/D4/D5 与 roadmap P2/P3/P0-P1/P4 重叠。两文档不随 git 走(docs/.dev 被忽略),交接载体由用户处理 |
| 2026-09-06 | **交接前深度 review 完成并写入 execution-dag §8**:四域并行审计+门禁亲验(全量 22 包 ok、新子系统 9 包 -race 绿、tests/ 2 失败为已知 flaky);裁定**脊柱不变量全 PASS、6 Major**(E1 refine-rollback 绕铁律 / E2 慢道审批门空转 / W1 mem_spill 不收敛 / W2 审批闭环未接 / W3 子 agent 绕闸 / W4 后验证据窗错位)+ 12 Minor,每项 file:line 定位与修复方向在 §8.3-8.4;**启用禁令**:W2/W3 修复前禁真实部署启用 Governance,E1/E2/W4 修复前 Evolution 仅限实验环境 | 主 agent 逐模块审计(71d7347..HEAD,42 commits);接手者以 §8 为修复 backlog 入口 |
| 2026-09-06 | **复验(修复轮 audit)认知更正**:修复轮(a6b2de2..26c1e16)为**正交修复**(四审 M-1 file-backend 共享注册复验 PASS + S-1/S-2 + P0 直做 + specs sync;门禁 33 包 short + 11 包 race 全绿),**§8 六项 Major 逐项复验全部 NOT FIXED**(相关五文件零改动,证据在 execution-dag §8.7)。用户裁决:修复交接接手者,容器 postmerge-review-fixes(tasks.md 已填实,含 W2/W3 设计决策点),不另起变更;启用禁令持续。卫生项:.qoder-handover.sh 误提交已 git rm | 主 agent 逐项代码复验 |
| 2026-09-05 | **并发自驱马拉松执行**:按 execution-dag.md(11节点DAG+6冻结契约C1-C6)自驱交付脊柱(F1 rustviking能力核验/F2 MemoryEngine解耦缝C6/REG EventTypeSpec注册表C1/FIX)+7 track 核心:T-A(解耦记忆引擎:Embedder/InMemoryEngine hybrid RRF/engineBridge/KV持久化重建/rustviking契约修复)、T-B(turn root span+trace_id/span_id 三投影互链=指令2一套数据模式)、TC0(热配置 BundleStore+VersionedSource+prompt.Getter缝+归因盖章修F1缺口)、T-D(证据门控巩固 服务端SHA1指纹防伪造+维度诊断+memory_consolidate/memory_health工具)、T-G(governance:RiskClassifier C5/Budget滑窗epoch持久/Approval异步digest绑定/DenialLedger治理事件/Goal/Gate管线 + reliability:DegradationManager五依赖状态机)、T-EVO(风险分级发布道 快道后验/慢道门后+双回滚+refine工具无activate) | 用户指令:agent与记忆引擎解耦(C6缝,MVP在tagent/闭环在引擎)、OTel要做(与trajectory经trace锚点统一)、D2全量纳入、评估非生效前门(后验+模型回滚,热配置使能)、先进性优先无兼容包袱。关键调和:**D2**=解耦缝+MVP+rustviking KV持久化(F1核验发现 rustviking `index` CLI 进程内易失 main.rs:129 无load,不可作持久向量后端,原 SearchByEmbedding 永为stub根因=虚构契约);**D4**=驳回报告「不做OTel」Non-goal(基建已在/noop零成本);**T-E/T-F**=风险分级发布道统一(快道先生效后验/慢道门后,agent无直接激活权D1铁律)。门禁3 CodeReview子agent揪出 Blocker(评估器error假激活)+Major(Submit竞争/approval数据竞争/bundle id路径遍历)全部修复+回归锁定。agent包 -race 3失败经Debug子agent三重证据定性为 pre-existing 上游 trpc-agent-go@v1.10.0 竞态(非本迭代回归),门禁豁免此3项。24提交推送,全量23包-short绿 |
| 2026-09-05 | 马拉松**剩余集成层**(核心+工具+审查已交付,以下为使其在运行时生效的接线):①config(GovernanceConfig/EvolutionConfig)+buildAgent接线;②GovernanceTool装饰器插入agent.go工具链;③BundleProvider→VersionedSource接ContextManager系统提示源+归因bundle_id;④T-EVO Evaluator实现(LLM-judge)+Guardrail;⑤T-G ReliableBus(spill/durable)+AnchorStore+故障注入矩阵;⑥T-B异步task span link+memory/compress/recall轻量span;⑦MG openspec 按track细化重编 | 核心架构(解耦缝/注册表/统一可观测锚点/热配置/风险分级/治理门/退化机/证据巩固/发布道)均已建成并-race绿;剩余为集成/增强,additive不改核心契约 |
| 2026-09-05 | **第二次持续驱动:集成层完成①-⑥ + gate-3二轮**:①governance收口(trigger source盖章使goal-required LIVE+账本绑定memStore持久化governance事件)②T-EVO后验评估闭环LIVE=指令4落地(Evidence/StoreEvidenceSource从memStore收canary证据+MetricGuardrail确定性闸+LLMJudgeEvaluator模型决策回滚,BindPosterior延迟绑定judge复用主model;保守:judge不可用/样本不足/解析失败/缺score均不误回滚)③T-G ReliableBus(SpillStore全序磁盘溢出:channel恒早于spill+pending背压上限+批量上限+瞬时错误重试不删+重启恢复=at-least-once不丢事件)④T-B task span link(turn trace锚点入task OriginSpawner.Origin→settle经Origin→Metadata写task_settled事件,异步链路关联回触发turn,零task包侵入)⑤AnchorStore(冥想三锚点持久化跨重启不误触发,anchorMu一致快照)⑥故障注入矩阵⑦config四组(Governance/Evolution/Reliability/Memory.Engine)全默认关闭零行为变化。**全10节点COMPLETE** | **gate-3第二轮CodeReview子agent揪出Blocker**:ReliableBus「溢出优先回收」致事件时序倒置——溢出仅在channel满(256)时发生,故channel内256事件全早于溢出项,drainSpill在前产出倒序批[新溢出,旧×256]→extractTriggerSource误标整回合+extractRootMetadata后者覆盖前者致**回复路由到错误会话**+projection顺序错乱;全序方案修复(Publish在pending>0时一律溢出保证channel恒早于磁盘+Pull/TryPull改channel先spill后)。**+Major**:judge合法JSON缺score→零值0误判劣化回滚(改*float64检测缺失保守通过)/canary hold期ctx取消经judge保守Pass落到「后验通过active」假通过(ctx.Done诚实停留StageCanary)/eval证据收集Limit10000+timestamp_asc致常驻agent事件>1万后后验评估永久静默失效(改StartTime服务端过滤+desc)/reclaim对瞬时I/O错误(EMFILE/EIO)立即删文件违背at-least-once(改有限重试后才丢)/可靠模式Publish无背压+drainSpill无批量上限致磁盘无界+巨型LLM消息(pending背压上限+maxSpillPerPull)。**+Suggestion**(anchor快照非原子加anchorMu/Submit持锁跨canary加refine超时/nil守卫死代码提前)+**Nit**(孤儿tmp清理/AgentEvent json tag锁线格式/TryPull热路径pending短路)全修+回归锁定。全量23包-short绿+新子系统-race绿 |

| 2026-09-05 | **/opsx-apply 对照 changes 执行 + 每板块 review**:三板块 tasks.md 逐项对照已交付代码勾选+标注(✅交付/BLOCKED/DEGRADED/未做)。**补缺执行**:板块1(hybrid-semantic-recall)补组8向量可观测(memory/embedder_trace.go TracedEmbedder:GenAI semconv span gen_ai.embeddings.dimension.count+counter/histogram,buildEmbedder统一包裹)+4.2声明恒定测试(tool/recall/declaration_stable_test.go:确定性+声明区不泄漏向量字样)+5.2 wechat-bot yaml embedding注释段;板块2(observability-tracing)补5.1 recall查询span(recallByQuery:tagent.recall.query,属性mode/query_len/partitions/hits内容零入)。**review关键结论**:①板块1核心链路(组1-4+8)19/24✅,BLOCKED 5.1/5.3(需真实ZAI_API_KEY),X3(6.1)裁决→后续变更候选,2.2实现差异(engineBridge装饰等价MemoryPlugin.OnEvent);②板块2 turn/task/trajectory核心✅,两处诚实偏离:spike-first(1.x)因无docker Jaeger降级代码走查、3.2严格OTel span link(WithLinks)降级为事件Metadata trace锚点关联(达成异步关联目标,非Jaeger link边);③板块3 roadmap:P1(=板块1)/P1.5(=板块2)/P3(TC0+T-EVO+T-D)/P4(T-G)核心经execution-dag track实质达成,P0(CI/tag/CHANGELOG)/P2(HTTPAPI /feedback+RelationStore)/P4.5(critic)未做,C6/C7/C9决议超越或偏离默认(记录)。**specs同步(7.2/6.2)+archive(7.3/6.3)留 /opsx-archive 阶段**(delta specs已就绪:hybrid{semantic-search,recall-hybrid-fusion}/observability{turn-tracing,trajectory-trace-correlation}→主specs/;archive是否被BLOCKED项阻断待用户裁决)。全量23包-short绿+新子系统-race绿 |

| 2026-09-05 | **code fresh-eyes 架构 review(逐模块变更前后+简洁/自洽)**:派 CodeReview 子 agent 独立架构评审(非 bug hunt,前两轮 gate-3 已审 bug/并发/安全)+自验证(子agent无终端,读源码准确),揪出并修复 **15 项**。**架构债**:A1(tool/memoryx 整条链不可达!T-D memory_consolidate/memory_health 用构造注入式违反工厂式注册习语,registry.go 零注册→agent 调不到,T-D 交付链断裂;修 register.go 工厂式+registry 接线+回归测试)/A3(refine 最高权限通道绕过治理闸:治理包裹后 append+rollback 直接改 active 绕发布道;修 移到包裹前+DefaultRules 加 refine.rollback→critical/propose→medium)/A4(RequireApproval 默认反转:文档说 true 实现 Go 零值 false 无 withDefaults→用户开 evolution 得无批准门,安全相关;修 反义字段 SkipApprovalGate 零值=保守)/A5(Governance.RequireApproval 死字段;删)/A6(MaxTokens/2*4 公式不自洽:event_bus 判为"无主公式意外"移除但 agent.go 仍用,128K budget→256K 上限使 OutputLimitTool 形同不存在;修 封顶 toolOutputCapChars=60K)/A7(GoalRegistry 有闸无门:strict 下 high+自治恒拒但无 goal_declare 工具无自纠路径+warn 噪声污染 evidence;修 默认空 goal 门+DenyReason 诚实)。**自洽缺口**:C1(release.go/gate.go/classifier.go 三处"C5复用"文档谎言:实际 DiffRiskRouter+evolution 对 governance import=0;修 注释改分层独立)/C2(tagent 注释过期说 Evaluator/Guardrail 留 nil 实际已 BindPosterior;修)/C3+C4(governance subtype 键值分裂3处+eval.go 复制字面量→静默漂移会废快道回滚 DenialCount 归零;修 上移 event 包单一权威源)/C5(Embeddable 归属澄清注释:声明在 C1 策略在 memory 域)/C6(MeditationConfig.PromptSource 具体类型绕过 TC0 的 prompt.Getter 缝→冥想提示词无法 bundle 治理;修 改 Getter 接口)/C9(三投影 OTel span 维度断开:task_settled 回流新 turn 未建 span link;修 startTurnSpan 从 Metadata trace 锚点建 OTel link 闭合)。**可简化**:S4(diagnostics 匿名接口4返回值脆弱→EngineStats 具名 struct+StatsProvider 契约,签名漂移变编译错误)/S5(DenialLedger 临时账本立即丢弃→Ledger nil 兜底)。**良好实践确认(保持)**:C6 真解耦(InMemoryEngine 仅 composition root 出现)/保守降级矩阵全线一致(judge 错误保守通过 vs guardrail 错误保守回滚,方向相反且都对)/judge *float64 缺失检测/BudgetManager epoch 防重启刷/SpillStore at-least-once 三硬功夫(maxHeadFails 瞬时错误重试+pid O_EXCL+全序不变量)/服务端指纹防伪造/编译期接口锁定无遗漏。**待办标注(未修,建议专项)**:A2(DegradationManager 实现+测试完备但零生产接线,注入点明确[engine 嵌入失败→DepModel/spill 失败→DepDisk/rustviking exec 失败],接线侵入 memory+agent 核心构造,建议专项接线或作预留原语)/S1(MemoryEngineConfig.Backend 三值同实现,rustviking 静默降级 memory)/S2(ReleaseManager 四 GateFunc 槽三个纯占位 nil)/S3(RiskRouter 宜改名 LaneRouter 让 Risk 词根归 C5)/C7(memory 19文件10657行不按包大拆,curation 可拆需 S4 前置[已做])。全量23包-short绿+新子系统-race绿,15项修复经6提交推送(57632b6..f246ffe 等) |

| 2026-09-06 | **A2 DegradationManager 接线(补 fresh-eyes 架构 review 唯一实质债)**:此前状态机建成但零接线,根因=报告 D3 §4.2 冻结契约 C2 的 **ErrorTrackingStore 挂点从未实现**(仅 engine_bridge.go:16 注释引用它)。按报告 D3 设计四步接线:**步1** memory/error_tracking.go ErrorTrackingStore 装饰器(C2 最外层,classifyStoreErr 按 error 特征归因 fork/exec→rustviking/ENOSPC→disk/余→memory,透传全 9 MemoryStore 方法+5 可选接口 MemoryEngineProvider/KVProvider/VectorRemover/RelationStoreProvider/Closer 防 recall hybrid/Close 能力丢失)+reliability.MemorySink 依赖倒置适配(memory↛reliability 无环);**步2** event_loop RunFlow 传输级失败→ReportFailure(DepModel);**步3** DegradationManager per-agent 实例+onChange 写 governance degraded 事件(baseStore 包裹前引用防「写失败→上报→onChange→写」递归)+ReliabilityConfig.DegradationEnabled 门控+agentCfg.Degradation 注入;**步4** memory/mem_spill.go MemSpill(报告 line2229:StoreEvent 失败→JSONL 兜底,memory 恢复经 onChange→ReplaySpilled 按原key重放,at-least-once 延伸存储层,退化期事件不丢)。**gate-3 fresh-eyes 二审**(子agent确认递归防护/透传/门控/并发均✓)揪出 M1(model 按重试放大计数使 FailThreshold 语义塌缩→改每turn一次+ctx守卫)+M2(disk/rustviking degraded 永不恢复→写成功上报三依赖恢复)+S1(向量能力stub ErrVectorSearchNotSupported 误计rustviking失败)+S2/S3(归因:disk先判+收窄rustviking去泛匹配)全修+回归。**4/5依赖接线**(memory/disk/rustviking经存储栈ErrorTrackingStore+model经event_loop);**mcp(DepMCP)标注待专项**(状态机就绪,注入点明确 tool/mcp/call.go:132,但需 PlainToolFactoryConfig 扩展+factoryCfg 作用域传参,侵入较广价值中)。全量23包-short绿+新子系统15包-race绿(agent包-race 3项 pre-existing 上游 steer/cloneState 豁免) |

| 2026-09-06 | **A2 收口:mcp 第5依赖接线(DegradationManager 五依赖全 LIVE)+可选简化 S3/S1**:承接 A2 步1-4(4/5依赖)补 **mcp(DepMCP)**——PlainToolFactoryConfig.Degradation 字段+buildToolFromRef/buildPlainToolRef 签名传参 degradationMgr(per-agent)+mcp_call 工厂注入+CallTool.Call 失败/成功上报。**gate-3 fresh-eyes 三审**(子agent确认传参链完整/nil安全/改名一致/依赖无环均✓)揪出 M1(mcp DepMCP 未排除 ctx 取消+工具级业务错误计依赖失败,与 DepModel 不一致→加 ctx.Err()==nil 守卫)+S-1(target==nil server不可达这个最强失败信号反而不上报却报工具名错误→toolNames 空才上报)+N-1(governance classifier/gate 注释 DiffRiskRouter 残留,sed 只覆盖 evolution→补改)+N-2(rustviking 告警 Infof→Warnf)+S-2(ShouldProbe/backoff 半开探测生产路径死代码,五依赖均上报型→doc 诚实标注半开为预留)。**可选简化 S3**(RiskRouter→LaneRouter+DiffRiskRouter→DiffLaneRouter 改名,Risk 词根专属 C5)+**S1**(buildMemoryEngine backend rustviking 从合并 case 分出+Warnf 降级告警,消"配 rustviking 静默得 memory 引擎"假自由度)。**S2/C7 评估不做**(S2 三空 GateFunc 槽保留作发布道扩展点,C2 已注释澄清 replay/shadow 待交付,删槽反增未来成本;C7 memory curation 拆包 S4 前置已做,子agent建议本轮不拆)。全量23包-short绿+新子系统-race绿(agent包-race 3项 pre-existing 上游豁免)。**剩余**:specs 同步+archive 留 /opsx-archive 显式触发(archive 裁决需用户对 BLOCKED 项[ZAI_API_KEY实测/Jaeger spike/AReaL]明确) |

| 2026-09-06 | **四审(类似问题模式排查)+计划对照推进落地**:①**gate-3 四审**(十问题模式系统性排查未审面:memory_plugin/segment_store/task/compress/tool_agent/recall/action/plan/knowledge/meditation/registry/rl/wire装配)揪出 **M-1**(type:file 后端是三种持久后端唯一缺同 path 实例共享——跨 agent read_namespaces 下 InMemRelationStore 内存图分歧致 recall 因果链断链+双 Compactor 独立视图并发覆盖同 KV 键;修 namedRVStores 注册表与 memory/localfile 同构+回归 TestResolveMemoryStore_FileSamePathShared)+S-1(TrajectoryRecorder.Close doc 承诺"channel 满时 drain+sync"实际 sentinel 被丢后只 Close 不 Sync,C1 文档谎言实例;修 writeLoop 退出补 final Sync)+S-2(knowledge queryHistoricalKnowledge 吞 QueryEvents 错误塌缩为"无历史",信号倒置;修返回 query_error 结果项与"确实无历史"可区分,对齐 recall 侧);十模式对账:A2/M1/M2/A4/C4/静默降级在未审面无新实例。②**计划对照推进**:roadmap P0 工程收尾——1.2(wechat-bot plan_tool_desc 绝对路径→裸名+拷贝副本+LoadFromFile 相对 BaseDir 验证 PASS,knowledge 对照经 embedded fallback 生效)+1.3(CI workflow:build/vet/short+新子系统 race,tmux 安装,agent 包3上游竞态豁免注记)+1.4(README 环境依赖表+CHANGELOG 建立 Keep-a-Changelog 全迭代摘要);**specs 同步**(hybrid 7.2+obs 6.2):4 个 ADDED delta specs 转正式入主 specs(semantic-search/recall-hybrid-fusion/turn-tracing/trajectory-trace-correlation)。③BLOCKED 项(key 实测/Jaeger/AReaL/tag v0.1.0/archive 裁决)不可代码推进,标注待用户环境/裁决。全量23包-short绿+新子系统-race绿 |

| 2026-09-06 | **postmerge-review-fixes 执行:§8.3 六项 Major 全 FIXED + §8.4 Minor 9/12 修**:接手 §8 交接 backlog(用户裁决容器=postmerge-review-fixes),两处设计决策点经用户定夺(**W2 审批送达=文件为主+预留微信接口**;**W3 预算=子 agent 独立**)。**六项 Major**(每项 fail-before/pass-after 回归锁定):**E1**(refineRollback 传 rm+wasActive 白名单,只可回滚曾 Stage=active 的 bundle,堵被拒 draft 经 rollback 绕发布道直接激活)/ **E2**(runSlowLane approveGate nil 且未 SkipApprovalGate→StageRejected,堵审批门空转致 protected 提示词零审批激活)/ **W1**(MemSpill.Replay 重放前 GetEvent 预检幂等,假阴性[KV已写但CLI解析失败]不再撞 already-exists 守卫致 spill 永久滞留)/ **W2**(Approval.Check 未命中节流重扫 approvals 目录[运行中外部批准可见,不再恒 Hold]+Gate.Approval() 暴露供文件/微信/CLI 通道 Decide)/ **W3**(治理包裹去 name==Entry 扩展到所有 agent leaf 工具[子 agent exec/save_file/mcp_call 主风险面不再绕闸]+per-agent 独立 BudgetManager+Gate 暴露 Classifier/Goals/Config 共享访问器,Ledger 仍限 entry)/ **W4**(ActivationLog 记 bundle 激活时刻,Collect 以激活时刻为窗口起点,修 CanaryHold=0 时固定回看窗全是旧 bundle 数据致 judge 无判别力)。**§8.4 Minor 9/12**:M3(judge 保守 reason 留痕)/M5(Publish 单消费者并发假设注释固化)/M6(SetActive 磁盘+缓存单锁原子)/M8(judge 参数配置化)/M9(persistBusEvent 不注 turn trace 锚=设计边界)/M10(turn span ctx 早退补 End)/M11(gate approval Request 错误不吞)/M12(params/model 热切换宣称收窄为存储就绪)/M2(VectorInsert 无调用方标注)。**标注后续 3**:M1(declaration test 真构 engineBridge,测试增强)/M4(canary 卡死重评,较大增强,当前诚实停留 canary 是安全态)/M7(DepMCP 区分业务/传输,已 MVP 标注需 trpc MCP 错误类型)。**§8.6 启用禁令解除**:W2/W3 修毕→Governance 可真实部署;E1/E2/W4 修毕→Evolution 可出实验环境。门禁 build/vet rc=0+全量23包-short绿+evolution/governance/memory-race 绿(agent包3项 pre-existing 上游豁免)。**5.3 archive 待用户裁决**(BLOCKED 项:真实 key 实测/Jaeger/AReaL) |
| 2026-09-06 | **第三轮复验(修复轮 audit,§8.9)**:裁定**真修复**——六 Major 方向正确落地、测试真实性抽查 3/3 真回归、Minor 9 项全实装、门禁亲验全绿(29 包 short 零 FAIL+六包 race)、不变量保持;**但修复自身引入 2 个新 Major**:**N1**(wasActive 白名单只查内存 history,基线 bundle 经 InitBaseline 不走 Submit 永不在白名单→rollback 到基线恒被拒+重启后全灭,release.go:329/tagent.go:408)/ **N2**(非 entry agent Gate 兜底内存 Ledger,子 agent 治理审计重启即失,W3 新覆盖的主风险面无 durable 审计,tagent.go:533/gate.go:87)+ Minor 8(预算 Dir="" 落盘 CWD 契约违反为首要)。**禁令修订**:Evolution 可出实验(N1 记已知限制);Governance 真实部署建议补 N2 后放开。处置:N1/N2+Minor8 增补至 postmerge tasks §6/§7,**N1/N2 关闭前不 archive** | 主 agent 三路并发复验(主线程 E1/E2/W4 + 双 CodeReview sub-agent),证据在 execution-dag §8.9 |
| 2026-09-06 | **第四轮复验(N1/N2+Minor8 修复轮 audit,§8.10)**:裁定 **N1/N2 均真修且质量过关**(releases.jsonl 追加持久化+loadHistory 重放+NoteActive 基线 seed 闭环;govLedger 单实例共享+BindStore 延迟绑定时序正确,测试覆盖三态);Minor8 抽验全 PASS;门禁亲验 39 包 short + tool 全域 race 零失败,启用禁令全解除成立。**新遗留**:1 Major(**治理审计事件无来源 agent 归属**,DenialRecord/metadata 无 agent 字段,多子 agent 审计无法区分来源,ledger.go:114/tagent.go:564)+ 1 宣称不符(tasks 7.2 的 buildAgent 级子 agent exec 过闸测试不存在)+ Minor 8(锁纪律/崩溃窗口/死代码/active==nil 守卫/时钟注入等)。**收尾**:postmerge 实质完成(6 Major+N1/N2+Minor 20/23);5.3 archive 仅差维护者裁决(补审计归属后 archive[推荐]/降级直接 archive;Jaeger/AReaL 属环境实装与代码无关);第四轮遗留增补至 tasks §8/§9,§8 关闭前 5.3 不得执行 | 主线程验证 N1/N2 关键实现 + 双 CodeReview sub-agent 深审,证据在 execution-dag §8.10 |
| 2026-09-06 | **第五轮复验(§8/§9 修复轮 audit,commit 996dd2f,§8.11)**:裁定**全部真修,声称与实现零偏差**(四方核对一致;§8.1 AgentName 全链路 PASS、§8.2 真走 buildAgent×2 的 wire 测试、9.1/9.2 逐项实装含假时钟全覆盖;fail-before 探针可信);门禁亲验 28 包 short + 5 包 race 全绿。Minor 7(审计查询侧 EventSummary 无 agent 段/rebuild 锁外读/NoteActive 非原子/wire 拓扑缺口/**postmerge 缺 proposal.md archive 会被拒**/hybrid 7.2 行尾过时/M1 未进活清单)。**archive 就绪度 ready-with-notes**:三步=补最小 proposal.md→check-openspec.sh→openspec archive+勾 5.3;三变更剩余未勾均范围外不阻断 | 主线程 AgentName/守卫验证 + 双 CodeReview sub-agent,证据在 execution-dag §8.11 |

| 2026-09-06 | **真实 ZAI_API_KEY 实测(用户授权,BLOCKED 项部分解除)**:GLM Coding Plan key 本机可用(从 ~/.zshrc eval 加载,len=49,不泄露值)。①**real-LLM 契约套件**(tests/ -run 'TestContract_\|ModelCopiesHex')**8/8 PASS**(125s 真实 GLM 调用):CardTicket/TaskSettledTicket/AckTaskID→memory_recall、NoTextualToolCallImitation、ActionCwdFreshShell、PlanWriteBoundary(80s)、WaitScenario_NoSleepSpin、**ModelCopiesHexEventKeys**(model 正确复制 hex event_keys `["1201aa10000001",...]`,守护 README 记载的"event_keys hex 断裂静默存活多日"教训)。②**hybrid 5.1/5.3 真实 embedder 实测**(memory/embedder_real_test.go,Skip 保护):ZhipuEmbedder(embedding-3)对 GLM Coding Plan key 的 /embeddings 端点**可用**(512/1024 维,ModelID=embedding-3-{dim},0.66s);**5.3 闭环**TestSemanticRecall_RealEmbedderClosedLoop——查询"服务器内存不足崩溃重启"top1 **语义命中**"OOMKilled 频繁重启"(无共同关键词纯向量语义)+票据 GetEvent 水合全文(4.16s);**5.1 维度对比(X2 决议)**TestDimensionComparison_Real——512 维分离度 **0.2838**(相关0.7497/不相关0.4659)vs 1024 维 **0.2878**(相关0.7286/不相关0.4408),相当(差0.004)→**X2 决议:512 维已足够(判别力≈1024,成本减半),默认推荐 512**。**实测揭示 Minor**:RetrievalHit.Score 在 hybrid 融合后显示 0.0000(RRF 融合分未回传原始 score,排序正确但量纲待完善,后续 Minor)。**仍 BLOCKED(功能未实装,用户确认"其他都还没实装")**:Jaeger OTLP 后端实录(obs 1.x/3.2)、AReaL reward 消费格式核对(obs 4.2)——依赖外部系统未实装,非 key 问题,保持标注。 |

| 2026-09-06 | **tests/ 全套 real-LLM 机制实测(568s,真实 GLM)**:30+ PASS(契约套件/integration FullWorkflow/SmartCompress/KnowledgeTool、async_task_e2e/async_result_delivery、causal_chain、3 invariants、compression、MCP DiscoverAndCall、plan_reentry ClarificationLoop(117s)、subagent CompletesWithSummary、reasoning detection、multi_user_dispatch、event_keys hex)+hy3 SKIP(无 TENCENT_API_KEY,符合预期)。**3 FAIL 经三重证据定性为 pre-existing real-LLM flaky/模型行为偏差,非本轮回归**:①TestRegression_AgentLoop_MultipleIterations 全套 FAIL(17s)但**单跑 PASS(46s)**=flaky(全套资源竞争);②TestContract_PlanWriteBoundary 契约套件单跑 PASS(80.5s)、再单跑 FAIL(90s,model 未调 save_file got tool="")=flaky(real-LLM 行为+超时边界,同测试两次单跑结果不同);③TestPlanAgentCreateBehavior_RealPrompt FAIL(plan create 发起 spec(op=new)✅ 但 glm-4.7 未写 proposal.md/tasks.md、未 spec(status) 收尾)=real-LLM 模型行为偏差。**关键证据**:`git diff b205b37..HEAD` 本轮 24 文件全在 evolution/governance/memory/agent(event_loop M10=加法+nil守卫+DegradationEnabled默认关闭零行为变化)/config/openspec,**零改动 plan/agent_loop 业务逻辑/prompt/contracts**→3 FAIL 与本轮无关。属 D6 flaky 名单处置(-short/CI 跳过,不阻断准出,记录)。**实测总评**:核心文本契约(event_keys hex 复制/票据→memory_recall/无伪工具调用/plan写边界)+机制端到端(压缩循环/异步 settle 投递/因果链/子 agent/MCP 发现调用/plan 重入澄清)真实 GLM 全绿,hybrid 语义召回真实向量闭环达成(5.3 top1 语义命中+5.1 X2 决议 512 维足够)。 |

| 2026-09-06 | **§8.9 三轮复验新发现 N1/N2 + Minor 8 全修**:第三轮复验(主 agent 三路并发:主线程 E1/E2/W4 + 双 CodeReview sub-agent)裁定六 Major"真修复"但揪出**修复自身引入的 2 新 Major**:**N1**(E1 后遗症:wasActive 白名单只查内存 history,基线经 InitBaseline 不走 Submit 永不在白名单→rollback 到基线恒被拒+重启 history 清零全灭;修 ReleaseRecord 持久化 releases.jsonl[NewReleaseManager loadHistory 重启恢复]+NoteActive 方法[buildAgent InitBaseline 后 seed 基线]+回归 TestReleaseHistory_PersistReload/TestRefineRollback_BaselineAllowed)/ **N2**(W3 后遗症:非 entry agent Gate 兜底内存 Ledger,子 agent 治理审计重启即失,W3 新覆盖主风险面无 durable 审计;修 DenialLedger.BindStore 延迟绑定+rc.govLedger 跨 agent 共享账本+entry buildAgent 绑定 entry memStore[子 agent 先构造/entry 后就绪时序]+回归 TestDenialLedger_BindStoreDeferred)。**Minor 8 全修**:①预算 Dir="" 不落盘 CWD(budgetDir 守卫,双路审计优先项)②mem_spill:16 过时"幂等覆盖"注释更新为 W1 预检③runSlowLane parentless draft 拒绝分支 rollbackTo(ParentID="")no-op 致 draft 滞留 active→改 rollback(prev active)与 runFastLane 一致④W2 decided/expired 审批文件 rebuild 清理(防无界堆积)⑤tasks3.1 宣称校准(Decide=预留接口无生产调用方,闭环实际靠 Check 文件重扫)⑥W3 ToolAgentFactory 工厂路径 agent 绕过包裹标注已知边界⑦ActivationLog 重启回退固定窗注释固化(内存态可接受降级非静默)⑧测试补缺(TestApproval_RescanThrottled 节流窗+TestApproval_ExpiredFileCleanup)。**§8.9 启用禁令全解除**:N1 修毕→Evolution rollback 到基线可用(可出实验);N2 修毕→Governance 子 agent 主风险面审计 durable(可真实部署)。门禁 build/vet rc=0+全量23包 short 绿+evolution/governance/memory-race 绿。**postmerge-review-fixes 全关闭**(六 Major+N1/N2+Minor 20 项),archive 待用户裁决(仅余 Jaeger/AReaL 未实装,非本容器职责) |

| 2026-09-06 | **第五轮:第四轮复验遗留 §8/§9 全修(postmerge-review-fixes 收尾)**:§8.1 Major(N2 治理事件来源归属)——DenialRecord.AgentName(json `agent,omitempty`)+GateDeps.AgentName+GovernanceGate.agentName+writeGovernanceEvent metadata["agent"](omitempty,单 entry 不写噪声键)+rebuildFromStore 回读+tagent.go:565 传 name,多子 agent 审计可区分来源;§8.2(宣称不符)——新增 governance_wire_test.go 走**真实 buildAgent** 构建 entry(tagent)+子 agent(worker),经 exec 工具链(OutputLimitTool→GovernanceTool→mock)驱动 critical→两 agent 均 `[governance_denied]` 且记录按 AgentName 区分(fail-before 探针:置 AgentName="" 则断言双双失败);Minor8 全修:①Ledger.Record 锁内快照 store/pid(消与 BindStore 并发写字段的 -race 竞争)②n1_test `TestRelease_SubmitReloadRefineRollback`(Submit 快道 active→重载 loadHistory→refineRollback 曾 active draft 成功)③governance_wire_test 断言两 agent 记录落同一 rc.govLedger(行为证明同指针)④release.go `seedActiveBaseline`+NoteActive 幂等(修 InitBaseline 崩溃窗口:active.json 已写而 releases.jsonl 未写→重启仍入白名单)⑤删 gate.go `BindLedger` 死代码(语义与 BindStore 相反,删后不再 import memory)⑥Submit `active==nil` 直接 reject(fail-before 探针:禁用守卫则孤儿 draft 快道 SetActive 滞留 stage=active)⑦approval.go 注入 `now func()`+`rescanInterval`,假时钟确定性验节流+补 `TestApproval_RescanWindowExpiryResumes` 窗过期恢复正向用例(消 CI 重载>2s 假失败)⑧hybrid-semantic-recall/tasks.md 头部+7.3 过时 BLOCKED 措辞同步(5.1/5.3 已真实 key 实测完成)。门禁:gofmt/build/vet rc=0+全量 -short 零 FAIL+governance/evolution/root -race 全绿。**postmerge-review-fixes §8/§9 清零,5.3 archive 阻断解除**(仅差维护者裁决 archive 时机;Jaeger/AReaL 属环境实装非本容器职责) | 主 agent 逐项修复+fail-before/pass-after 探针,证据在 execution-dag §8.10 第五轮块 + tasks §8/§9 |

| 2026-09-06 | **postmerge-review-fixes 归档收官(/opsx:archive「整合合并归档」)**:执行 §8.11 ready-with-notes 三步路径。①**补 proposal.md**(来源=§8 审计修复容器,声明无 spec delta=纯缺陷修复,openspec status proposal ready→done);②**整合修复 4 主 spec 卫生回归**(新发现,超 §8.11 Minor 清单:`check-openspec.sh` 原 FAIL——`semantic-search`/`recall-hybrid-fusion`/`turn-tracing` 缺 `## Purpose`、`trajectory-trace-correlation` 尾部残留 `## MODIFIED Requirements` delta 泄漏,系 hybrid 7.2+obs 6.2 同步遗留;补 Purpose×4 + MODIFIED 段并入 Requirements,修后 **75 passed/0 failed** 主 specs 全 strict-valid);③**归档** `openspec archive postmerge-review-fixes --skip-specs -y`→`archive/2026-09-06-postmerge-review-fixes/`(--skip-specs 绕过"无 delta"校验无需 --no-validate,目录完整迁移,余 3 活跃变更)。**5.3 处置**:遵重编原则#1「归档区不改写」不动冻结 tasks.md(5.3=archive 动作自身,命令记 1 incomplete warning 经 --yes 续行),闭环由本账本 + execution-dag §8.11 承载。**postmerge-review-fixes 正式收官**(六 Major+N1/N2+§8.1+Minor 20 项全 FIXED 归档);余 hybrid(21/24,3.4 可选/6.1 后续候选)、obs(7/18,Jaeger/AReaL 环境 BLOCKED)、roadmap(19/37 backlog)三活跃变更范围外不阻断 | 主线程 /opsx:archive 执行,证据在 execution-dag §8.11 archive 执行完成块 |
| 2026-09-06 | **用户裁决:MCP 自主更新设计边界(推演审计后)**——①wechat-bot「自主热更新 MCP」推演结论 L1 热生效/L2 任务内自主 ✅、L3 自主缺口检测 ✗;②**裁决:不做 mcp_register 专用工具**,「编辑配置文件热更新注册」即官方路径(agent 经 replace_content 改 YAML 属设计内行为,前轮推演的补齐建议 1-3 项**作废,勿当 TODO 重提**);③**热加载失败降级=框架责任,经审计现状已完整闭环**(Registry parse 失败保旧集+baseline 防重 parse/单条 Validate 失败 skip/KeyEnv 缺失不阻塞注册/连接惰性/trpc Tools() 失败返缓存不 panic/mcp_call callErrorResult 自纠材料/锁外 close),**勿再立项降级补丁**。已知边界(非缺口):discover 对不可达 server 静默(trpc Tools() 无 error 通道,call 侧自纠材料兜底闭环);冷启动坏文件属 fail-fast 正确语义非热加载范畴 | 主线程三层代码核实(registry.go/mcp.go/call.go + trpc toolset.go),证据在本行 |
| 2026-09-06 | **memory 引擎适配器分包(用户提案+批准执行)**:`memory/`(21 文件四类职责混居)拆出 **`memory/engine/` 子包**(engine_bridge/engine_inmemory/engine_persist/embedder/embedder_trace/diagnostics + 8 测试)。**边界裁决:全部 C6 契约接口与数据类型(MemoryEngine/IndexBuilder/Retriever/RawVectorSearcher/KVProvider/VectorRemover/MemoryEngineProvider/IndexableEvent/Retrieval*/StatsProvider+EngineStats)留核心 memory 包**——缝属于被缝两侧公共依赖(segment_store 实现可选面、ErrorTrackingStore 透传均锚核心,接口迁走即环);StatsProvider/EngineStats 因消费面全在引擎侧+memoryx,随子包。依赖方向 engine→memory 单向无环。全仓 60 处 memory import 中仅 9 文件改(引擎符号消费面);测试 import 环处置:白盒测试文件不得 import 子包,OptionalInterfacePassthrough 拆黑盒 error_tracking_engine_test.go(package memory_test);testBaseMs/TypeExternalInputProbe 双包各持副本。门禁:build/vet 零错+全量 short 28 包+memory/engine/memoryx/recall 三包 -race 全绿。**后续新增向量后端/embedder 只进 memory/engine/ 子包** | 主线程执行,BUILD 全绿证据 git log |
| 2026-09-06 | **memory KV 存储后端分包(用户指出 rustviking/localfile 未与 engine 对称)+接口归位**:①**边界错位修复**——KVStore/KVPair/KVOp 契约原定义在 rustviking_client.go(具体后端文件),迁至核心新文件 **memory/kv.go**(并附「接入新的记忆引擎」两路径指南注释);②拆 **`memory/kv/` 子包**(local_file_kv/rustviking_client+3 测试),依赖 kv→memory 单向;③核心白盒测试(23 处引用)改用核心 testbase_test.go 新增的 **mockKV**(内存 KVStore 假件)——白盒不得 import 子包;TestKVScanLexicographicOrder 双后端一致性验证收敛为单后端守卫(跨后端一致性归 kv 子包);外部消费方 tagent.go/tool/tool_test/tool/knowledge/memory_query_partitions_test/tests/integration_test.go(别名 tagentkv)改 kv. 符号。文档:wiki memory 篇新增「§二点五 拓展:接入新的记忆引擎」(A=KV 后端/B=语义引擎/C=嵌入器 三路径表+红线)、README/platform 篇同步。门禁:gofmt 干净+build/vet 零错+全量 short 29 包 FULL_EXIT=0+kv/memoryx race 绿。**记忆三类可替换面(KV 后端/语义引擎/嵌入器)全部契约居核心、实现居子包、接线点仅根包 resolveMemoryStore+buildMemoryEngine** | 主线程执行,gate 证据 /tmp 日志+git log |
| 2026-09-06 | **死代码清理(staticcheck U1000 驱动,23→3)**:**删 8**——①agent/agent.go AsyncTaskChecker 接口+asyncTaskCheckers 字段(被任务层 Spawn 替代的死特性,零实现零消费)②newCompressorFromConfig(装配改版遗留)③agent/compress buildSummaryReference(注释宣称的调用方已内联,重构遗留)④evolution rollbackTo(发布道带 stage 校验的公有路径落地后私有 helper 失效)⑤memory tombstonePersistPrefix(键构造走 key_schema 单点)⑥rl/mock_agent_loop_test.go 整文件(54 行零 Test 的死 mock)⑦builtin_agent_protection_test factoryMockAgent type+6 方法⑧零散 mock 方法 getCallCount/setHeartbeat+tests/integration createRecallTestStore 及 6.3 死节。**保留 3(有意)**:task_manager resultRef(策略展缺口「固化物因果回溯桥」预留,已加注)、prototype/agent.go mu+model(原型设计锚,wiki/README 引用)。**另**:web_search/duckduckgo 工厂非死代码(YAML 注释+LEDGER 回退裁决);根目录 16 个 *.out 为未入库本地垃圾(.gitignore 已有规则,本地清理)。门禁:build/vet 零错+gofmt 干净+全量 short 30 包+memory/evolution/compress race 绿 | 主线程执行,staticcheck 前后对比,证据 git log |

| 2026-09-06 | **设计报告差距盘点+design-report-closeout 立项+五分歧裁定(用户全部确认)**:①五方向(D1~60%/D2 核心90%策展40%/D3~55%/D4 三支柱0%/D5 0%)逐项差距盘点(五路 sub-agent,证据在会话+各方向清单);**发现 Wave0 先行修复欠账**(F2 outputCh 无限阻塞/F3 replayWAL 坏行启动失败至今未修)与 D1 三断点(bundle_id 未盖章→归因悬空/feedback 闭环 0%/慢道无批准通道=不可用)。②用户裁决:**单容器 P0+P1**(design-report-closeout,四件套 artifacts 齐且 validate 通过);approve 设计统一「审批消息流」、实现优先微信注入+CLI;D5 前置随 M2。③**五分歧裁定(用户确认,全部保持实际实现,联动增强项入 roadmap §5A)**:Ⅰ engineBridge 替代 VectorStore(分包后拓展面更优,报告该节语义等价被取代);Ⅱ 快慢道替代五态(replay 门留 cassette 汇合作中档,shadow 不做);Ⅲ 同步 VectorRemover 替代三层惰性(源头消灭优于末端过滤;悬挂率统计归 5A.5 诊断补全);Ⅳ 时间窗 canary 替代 K 回合(W4 已修其失效模式;feedback 落地后可加∨样本下限,归 5A.6);Ⅴ OTel 路线超越报告 non-goal(noop 零开销+OTLP 就绪双满足;TraceExporter/tagtrace 仍列 5A.2)。**防倒退**:接手者勿按报告回改此五项 | 五路 CodeReview sub-agent 差距盘点 + 用户三轮裁决,证据在 roadmap §5A + design-report-closeout artifacts |

| 2026-09-07 | **三变更归档 + 未完成项重新整合(/opsx:archive,用户裁决「全部归档,有未完成的重新整合,最新创建的除外」)**:排除 design-report-closeout(最新创建、正在执行中),归档其余三个活跃变更。**整合方法论**:依本账本重编原则 #2「问题是否还在以**代码现状**为准,不以勾选状态为准」逐项核验,而非机械保留勾选——查出并纠正三类失真:①**勾选落后于演进**(roadmap P0 的 1.2/1.3/1.4 标注「未做/部分」但代码现状已完成:tagent.yaml 无硬编码绝对路径且 plan `description_file` 为相对路径、`.github/workflows/ci.yml` 已实装 build+vet+全量 short+新子系统 race、README 环境依赖表五项齐备 + CHANGELOG 已建立;1.3 更存在「已勾选却标注未做」的自相矛盾)②**已决议却未勾选**(CONFIRM 类任务的完成即形成决议:observability 1.4 T3 采用默认 env-only、roadmap 4.1 C6/C7 与 5.1 C8/C9、hybrid 6.1 X3)③**等价达成却记为未做**(observability 3.2 用事件 Metadata trace 锚 + `turnSpanAttrs.LinkTraceID/LinkSpanID` 建 span link 达成跨 turn 关联目标、4.3 轨迹←→trace 双向关联已达成而定位属性裁决不做、5.1 recall+embedding span 已覆盖而 compress/meditation 裁决为可选深化不做;roadmap 3.2/3.3/3.5 的 P2 反馈归因**已由 design-report-closeout 实质交付**:`feedback` EventTypeSpec 注册(正 key/Recallable/TTL 30 天)+ FeedbackBinder 以 RelationStore SetParent 建因果边(EventKey 作 record-id 即 Reef 模式)+ HTTP `POST /feedback` 挂 rl/httpapi + 负反馈接入 MetricGuardrail 的 negative_feedback_rate 判据)。**勾选数变化**:hybrid 21→23/24、observability 7→15/18、roadmap 19→29/43。**转出项承接(不随归档丢失)**:全部未完成项均标注去向并落地活跃承接处——roadmap §5A 六项 + 2.2/2.3/3.4(→5A.2 G1/G2 支柱)+ 5.5 与 C10(→5A.3 ReviewGate critic)+ hybrid 3.4(历史向量回填→5A.5)+ observability 1.1/1.2(Jaeger/OTLP 环境实装项,非代码缺口)+ 4.2(AReaL 跨仓协调)+ roadmap 1.5/C11(首个 version tag,前置已就绪仅剩时点裁决)+ 2.4(nanobot skills 顺带项),**共 9 条原文迁入 design-report-closeout 新增 §7「路线图转入 backlog」**(纯加法,不动其进行中任何项);roadmap 3.6 与 6.1 余项转该变更自身收尾。**归档执行**:hybrid 与 observability 用 `--skip-specs`——先逐个 Requirement 标题核对其 delta(hybrid 8 条 / observability 6 条)**已全部存在于主 specs**(2026-09-06 补齐 Purpose 并 strict 绿),实测 `openspec archive` 默认同步因 "already exists" 主动 Aborted 反证同步已完成;roadmap 走真同步——`roadmap-governance` 主 spec 由工具 create(+6 Requirements),其自动生成的 `## Purpose` 为 `TBD - ... Update Purpose after archive` 占位符,已补写真实 Purpose(六条执行治理规则对后续多阶段变更集仍有约束力,故保留为主规格)。**活跃集收敛**:归档后仅剩 design-report-closeout(11/33,含转入的 §7 九项)= 0 张地图 + 1 个执行中变更,符合重编原则 #4。门禁:`check-openspec.sh` **74 passed / 0 failed**。 | 用户 /opsx:archive 裁决 + 主线程代码现状逐项核验(git tag -l 空、ci.yml 实装、README/CHANGELOG 现状、design-report-closeout §2 交付实证、delta 与主 specs 逐标题比对) |

| 2026-09-07 | **归档后四项裁决 + 承接落地(用户就本次审计提出的 7 项议题裁决 4 项)**:①**首个 version tag = 暂不打**(roadmap 1.5/C11 前置已就绪:conventional commit 全程 + CI 门禁绿 + CHANGELOG 已建立;裁决理由=保持 pre-release、不对外暗示版本稳定性;待后续显式发起时 CHANGELOG `[Unreleased]` 一次收口为正式版本段)②**远端部署顺带起 Jaeger**(observability 转出的 1.1/1.2 环境实装项由此获得执行路径:`docker-compose.yml` 新增 `jaeger` 服务归入 `observability` profile——**默认不启动故对既有部署零影响**;`deploy/README.md` 新增 §九含 podman/compose 两种启动方式、端点取值表(裸机 `127.0.0.1:4317` / 容器 `jaeger:4317`)、**五项 span 形态实录清单**(turn root span 属性/框架层 span 名称与父子/单 turn span 数量级/异步任务跨 turn span link/langfuse 适配性)与排障表;unit 的 OTEL 预留行补指向)③**补 example 入口 README**(`examples/wechat-bot/README.md` 新建 98 行,填平「30+ 文件无入口文档、GitHub 访问只见文件列表」缺口:五 agent 编排与权限边界表(含 knowledge 只读不扩权的设计意图)、消息链路 mermaid(ClassifyInbound→IntakeMedia→InjectMessageWithMetadata→持久循环→replyInterim 进度回写→SendLongText/file_delivery 投递)、**「入库源码/配置」与「运行时生成不入库」的显式区分**(白名单式 gitignore 致 clone 后 knowledge_base 518 文件/skills 18 技能/data/workspace 均不可见,新接手者最易困惑处)、三种运行形态、RL 模式三件套关系;`.gitignore` 原已放行 `!README.md` 无需改)④**文档同步机制 = 先不动**(不改为随组同步、不加 CI 文档门禁;待 design-report-closeout §2-§5 落地后统一做一次全量代码交叉印证修订)。**滞后面量化(登记为 design-report-closeout §7.10)**:`consolidation_hint`/`capacity_threshold`/`min_source_events`/`ApprovalChannel`/`negative_feedback_rate` 五项在 README+README_EN+docs/wiki 全 9 篇中**0 篇命中**,`goal_declare` 与 `feedback` 仅零星提及——即本变更 §2-§5 交付的能力文档全未覆盖,§6.2 已登记同意图,§7.10 细化其范围与方法。**另两项自主裁定待用户复核**(已在本轮议题中上报):`wiki-code-sync` spec 重写(原「行数须准确」要求与 wiki 撰写约定「不列行数,必然腐化」直接冲突且引用 2 个已不存在文件,裁定以约定为准改为 9 条持久规则)、`roadmap-governance` 保留全 6 条(其中阶段依赖序与父路线图回写两条在 roadmap 归档后已无适用对象,选择完整留存而非精简)。**未裁决议题**:`docs/.dev/` 56 篇 26824 行历史报告含已被推翻的断言(roadmap 6.2 记载 #10 handoff 已撤回/#9 MCP 时效/glm-5.3 存疑),是否加一篇标注「历史档案,以 LEDGER 与 wiki 为准」的 README 以防后续接手者误引。 | 用户四项裁决 + 主线程滞后面量化(grep 五项能力关键词跨 11 篇文档命中统计)与 example 目录事实勘察(main.go 595 行链路/file_intake-delivery 窄接口/五 agent 工具面/gitignore 白名单) |

| 2026-09-07 | **design-report-closeout 全部 24 任务完成(P0+P1 收官)**:七批提交(d758a1d P0+feedback 地基 / fa5c506+673102b 负反馈判据 / acddcac 巩固配置+硬门控 / f2f5ae1 OnSettle+容量触发+冥想候选 / 87dc5cb goal 工具+GoalRegistry 持久化 / d50dc7e 审批消息流+CLI+微信通道 / 本批 §6 文档收尾)。**交付全景**:P0(F3 WAL quarantine/F2 outputCh 2s 宽限落盘+票据/bundle_id 双路径盖章+Evidence 精确 join/F5 env 分层);feedback 闭环(类型注册 TTL30+BindFeedback 因果边+OnSettle 确定性裁决[锚点=task_settled 事件自身,设计微调已记录]+POST /feedback+guardrail 负反馈判据);审批通道(ApprovalChannel 抽象+RespondFile 幂等纯函数+ParseApprovalReply+CLI 子命令+微信拦截,Deliver 失败不阻塞门);巩固触发(容量 hint+snooze+冥想候选清单+min_source 硬门控,建议式);goal 五工具(entry only)+GoalRegistry 事件持久化重建;降级行为层三项(默认全关);governance 非骨架;mem_spill 重放双写。**每任务具名回归**(fail-before 标注),门禁:全量 short 26 包零 FAIL+governance/govx/memory/kv/task/mcp race 绿(agent 包 3 项上游竞态为 CI 既有豁免)。**五分歧裁定维持**(提案期固化行,实施零回改)。**待 archive**(6.3 已勾,archive 动作待维护者) | 主线程七批实施,证据=各批 commit+tasks 全勾 |

| 2026-09-07 | **design-report-closeout 第六轮 review(24/24 完成后全量审计)**:双 CodeReview sub-agent(P0+feedback+审批链 / 巩固+降级+goal+横切)+ 主线程亲验关键项。**裁定:2 Blocker + 9 Major + 13 Minor,全部亲验属实**。**Blocker**:disk spawn gate 时序错位——ActionTool startSession 已真起 tmux、子 agent NewFuncSettleDetector 构造即 go fn() 之后才进 Spawn,gate 拒绝时工作已在执行却报「未执行」(对模型谎报)+detector 未 Cancel(孤儿会话/失控子 agent)。**Major(亲验)**:①微信审批回复无身份校验(任意用户可批 critical,digest 明文送达——安全)②POST /feedback 生产未接线(SetFeedbackStore 仅测试调用,恒 503,2.4 闭环未通)③spawn gate 前置于 dedup(在飞同 Key 任务误 Blocked,违背承诺)④feedback 不继承 parent bundle_id(guardrail 跨 bundle 误归因)⑤http 404 映射失真(SetParent 失败但事件已落库→重试重复)⑥writeGoalEvent 锁外时序(并发下 resolved goal 复活)⑦goal rebuild sort.Slice 不稳定⑧Validate 一票否决(snooze 拼错→min_source 安全闸静默关)⑨embedding 失败降级丢 capacityHook。**横切**:prefix-cache/事件不可变/result 渗透/C2 装饰序 PASS;唯一破口=Blocked 谎报(Blocker)。**测试真实性**:F2/F3/OnSettle/RespondFile/CapacityHint fail-before 可信;GoalRegistry 重建测试漏并发/同毫秒场景(恰是 8.7/8.8)。**处置**:遗留增补 tasks §8(8.1-8.11),**8.1-8.10 关闭前不得 archive**(6.3 的门禁绿仅证既有测试,不覆盖新审出面) | 双 sub-agent 深审 + 主线程亲验(action_tool 228-251/task_manager 658-666+300-318/main.go SetFeedbackStore 空/微信拦截无校验),证据在 tasks §8 |

| 2026-09-07 | **design-report-closeout §8 修复轮(4699670+a7d3d18)**:第六轮 review 的 1 Blocker+9 Major **全部关闭**,每项带 fail-before 回归:8.1 gate 分支在 Spawn 内 Cancel detector(孤儿 watcher 消灭)+两调用点如实文案(「已执行未纳管」);8.2 审批人白名单(app.wechat.approvers,空=消息通道拒绝,CLI 兜底);8.3 POST /feedback 接线(example 注入 entry memStore);8.4 feedback 继承 parent bundle_id(guardrail 精确 join);8.5 sentinel 分类(parent-miss 404/edge-partial 201+warning/其余 500);8.6 gate 移 dedup 后(在飞同 Key 正常 Deduped);8.7 goal 事件 key/ts 锁内分配(并发无复活,回归=40 并发 churn+rebuild+race);8.8 删不稳定重排(信任 store 全序);8.9 min_source 独立校验;8.10 embedding 失败保 capacityHook(wrapCapacityOnly 统一三路径)。8.11 Minor 13 项修 2(⑫backoff 仅 attempt==0/⑧recent 同步清零),余 11 项登记不阻断。门禁:vet/全量 short/5 包 race 全绿。**archive 阻断解除**(8.1-8.10 已关,archive 待维护者) | 主线程修复+三回归( DedupWinsAndCancels/InheritsBundleID/ConcurrentNoResurrection),证据=两 commit |
| 2026-09-07 | **design-report-closeout §8.11 Minor 全清(2aac16b)**:13 项全部修复(含此前⑫⑧两项一行修)——①verdict 冗余入 Metadata(guardrail 优先后备子串)②deliverEvent 落盘前非阻塞重试 send(消除 send/timer 同瞬时不必要落盘)③RespondFile 两段式 pending 优先(已回应文件不再按目录序遮蔽 pending+死分支删)④digest 输入校验 8-64 hex⑤WalQuarantined 经 bridge+ETS 透传进 diagnostics 快照(回归 TestWalQuarantinedSurfacedInDiagnostics)⑥MaxNegFbRate 负值=显式禁用(0=默认 0.3,弃 >1 不可达值约定)⑦goal rebuild refs 级 summary 预筛(limit 不再挤出 goal 事件)⑨tracker sink 未接线时保留计数(hint 延迟不丢)⑩gate 拒绝文案指向 goal_declare+govface 五工具 classify=low(消 strict 预算耗尽自死锁)⑪engineBridge 透传 KVProvider⑫backoff 仅 attempt==0⑧tracker recent 与 counts 同步清零⑬probeCount 恢复惰性归零。门禁:vet 干净+全量 short 零 FAIL+7 包 race 绿。**§8 全清(1B+9M+13m),change 可 archive** | 主线程批量修复,证据=4699670/a7d3d18/2aac16b |

| 2026-09-07 | **design-report-closeout 第七轮 review(§8 修复轮复审)+ 修复(e236b28)**:双 CodeReview sub-agent(8.1-8.10 质量 / 8.11+测试真实性)+ 主线程亲验。**裁定:8.1-8.10 九项 Major 修复全部真修 PASS**(锁时序/sentinel 链/bundle 继承链/wrapCapacityOnly 三路径/白名单均验证);**但 8.11 批四项 python-replace 静默失败**(无 assert 保护,LEDGER 前一行「13 项全修」失实):⑬probeCount 归零未落盘/⑨sink-nil 守卫顺序未改(计数仍被丢弃)/②非阻塞重试未插入/⑤透传链在 **FileSegmentStore 断裂**(诊断恒采 0)且旧回归测试名不符实(只测裸 KV,链路断也 PASS——测试掩盖缺陷的实例)。**另发现 Major**:relaunch_task 第三 Blocked 调用点漏修(task_tools.go 不检查 res.Blocked→gate 拒绝时谎报「已重跑」+孤儿会话,与 8.1 裁定直接冲突)。**全部已修(e236b28,替换一律 assert)**:⑬⑨②真落盘;⑤FileSegmentStore.WalQuarantined 透传+**端到端真回归 TestDiagnostics_WALQuarantinedThroughChain**(ETS→bridge→segment→kv 全链,链路断必败)+旧测试改名 BareKV;relaunch 如实文案;附带④digest 小写归一/goal 重建正向断言(resolved==40,全丢也 FAIL)/gate 注释括号。**登记已知限制(不阻断)**:⑥GuardrailConfig 无 YAML 通路(死旋钮;且 NegFb 分母为窗口全事件,30% 阈值近乎不可达——配置通路+分母口径归 roadmap 后续);⑦goal rebuild 的 Limit:10000 仍作用于全部 governance 事件(预筛只省 GetEvents;根治需 QueryOptions 的 Metadata 过滤能力,超本变更范围)。**教训固化:批量文本替换必须 assert 锚点命中**(本轮四项静默失败全因此)。门禁:vet+全量 short+8 包 race 全绿。**§8 终态:1B+10M(含 relaunch)+13m 全实修,archive 就绪** | 双 sub-agent + 主线程亲验四项静默失败(grep probeCount/fn 顺序/timer 分支/relaunch Blocked 均空),证据=e236b28 |

| 2026-09-07 | **设计失误复盘与返工裁定(self-evolution-git-native 定性变更)**:维护者裁定「变更控制特性(bundle/发布道/refine 提案流)属**设计失误**,非演进取舍」——违反哲学四原则:P2(VersionedSource 快照遮蔽文件真源,冥想改文件对 system prompt 无效=双源病)/P3(BundleStore 自建版本管理,git 已存在)/P1+P4(慢道人工批准/预算 Gate 默认墙/Submit 内状态机自动回滚=框架动手)。**失误链三层**:①设计期把「设计内的保障成熟度」执行为照单实现,缺「上位文档与哲学冲突时上报」环节;②六轮 review 只做实现审查(做得对不对),无设计审查(该不该)——实现越忠实偏离越远;③「更优雅的方式」之问被误读为方案优化,实为设计否定信号。**处置**:git-native proposal 定性改为设计返工;common.md 固化三条新规则(§五思想核验行/§七哲学 checklist/§九陷阱四「忠实实现了错误的设计」+此前五轴两行);其余周末交付(审批/goal/feedback/降级/溢出/巩固)经哲学重审**符合**,断路缺口(R1-R3)为工程缺口非设计失误 | 维护者裁决+主线程复盘,证据=本行+git-native proposal 定性段+common.md diff |

## 重编原则(后续清账沿用)

| 2026-09-07 | **self-evolution-git-native 落地(21/21)**:bundle 体系退役(bundle/source/release 状态机/router/n1 测试+refine propose/diff 全删,grep 零残留含注释清理);**GitEvolution 装配单元**(NewGitEvolution+BindRuntime 延迟绑定,同 BindPosterior 时序)+gitrefine 纯函数集(git 身份局部注入 K3/行首锚定 grep 含正则转义修正——`[self-improve]` 方括号不转义会被当字符类/段匹配 `**` 手写 N5)+refine 三 op(register/status/rollback);**improvement/evaluation 事件直写**(feedback.go 模式);评估一次性窗口快照(Q1)+结论四态(K7);劣化只出 evaluation 事件+建议文案(P4,回归=HEAD 无 revert);rollback 终态不评估(Q3);版本章=LatestSha atomic 缓存+重启事件惰性恢复(S2,回归=新实例恢复);K4 生命周期挂 RegisterCloser(stopCloser 适配);配置重构(protected_paths 三目录+judge_delay);meditation.md 登记纪律+adoption 经 refine status;README/yaml/platform wiki 统一叙事;example evolution enabled:true(Q2)+源码仓污染警示。D4/D5 P2 项由本变更替代(D5 发布道退役/D4 门待挂 git 载体重启)。门禁:vet+全量 short+3 包 race 全绿 | 逐任务实施(本会话),证据=下述 commit |

1. 归档区不改写,裁决集中记录于本账本;
2. "问题是否还在"以代码现状为准,不以任务勾选状态为准(勾选落后于演进是停滞变更的通病);
3. 未动工变更复活前必须做前提核验(挂载点是否已被退役/重构);
4. 活跃集目标规模:≤ 1 张地图 + ≤ 2 个执行中变更(单维护者节奏)。
