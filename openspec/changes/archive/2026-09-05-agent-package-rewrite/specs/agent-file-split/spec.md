## 能力: agent-file-split

将 tagent_agent.go (1232行) 拆为 7 个职责文件，保持在 agent/ 包内。

## 需求

### 拆分映射

每个函数归属唯一一个目标文件，不允许跨文件移动后再引用私有函数。

### 约束

- 拆分后所有测试必须通过（零行为变更）
- 不修改任何函数签名或公有 API
- 不引入新的 struct 或 interface
- 每个文件 ≤ 300 行（agent.go 可能 ~350 行因含结构体定义）
